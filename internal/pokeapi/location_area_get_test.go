package pokeapi

// TODO: test that the first page of results gets properly cached on the first search
// example: map -> map -> mapb -> test if cache was used

// TODO: make the generic api get function. All the get files are repeated code where the only thing
// that changes is the type
